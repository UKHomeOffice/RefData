---
apiVersion: v1
kind: Service
metadata:
  labels:
    name: {{.REFDATA_NAME}}-service
  name: {{.REFDATA_NAME}}-service
spec:
  ports:
  - name: https
    port: 443
    targetPort: 8443
  selector:
    name: {{.REFDATA_NAME}}
